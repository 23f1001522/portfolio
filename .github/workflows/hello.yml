name: Hello Action

on:
  push:
    branches:
      - main  # Trigger the action when you push to main branch

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1001522@ds.study.iitm.ac.in
        run: echo "Hello, world!"
